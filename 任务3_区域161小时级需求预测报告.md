# 任务 3：区域 161 小时级出行需求量预测

> **方案**: A（区域 161 + 小时级粒度）  
> **模型**: PyTorch 神经网络 vs 随机森林  
> **数据**: `yellow_tripdata_2023-01_cleaned.parquet`  
> **分析脚本**: `taxi_demand_prediction_hourly.py`

---

## 1. 任务设定

### 1.1 目标
预测 **区域 161** 每小时的出租车出行需求量。

### 1.2 数据规模
| 项目 | 数值 |
|:---|:---|
| 原始记录数（区域 161） | 134,422 条 |
| 小时级样本数（含补零） | 745 条（31 天 × 24 小时） |
| 特征构造后可用样本 | 721 条 |
| 训练集 | 576 条（80%，时间顺序前段） |
| 测试集 | 145 条（20%，时间顺序后段） |
| 测试时间范围 | 2023-01-25 00:00 ~ 2023-01-31 23:00 |

### 1.3 特征工程（14 维）

| 特征类别 | 具体特征 |
|:---|:---|
| 时间基础 | `pickup_hour`, `dayofweek`, `day`, `is_weekend`, `is_rush_hour` |
| 滞后需求 | `demand_lag_1`, `demand_lag_2`, `demand_lag_3`, `demand_lag_6`, `demand_lag_12`, `demand_lag_24` |
| 滑动统计 | `demand_roll_mean_3`, `demand_roll_mean_6`, `demand_roll_std_3` |

> **构造逻辑**：滞后特征捕捉短期自相关性；滑动均值平滑噪声；滑动标准差反映需求波动幅度。

---

## 2. 实验结果

### 2.1 性能对比

| 模型 | MAE | RMSE |
|:---|---:|---:|
| **PyTorch 神经网络** | 21.24 | 31.09 |
| **随机森林** | **19.88** | **27.50** |

> 基准：区域 161 平均每小时需求量约 173。两种模型的误差约占均值的 **11–12%**，在小时级时序预测中属于良好水平。

### 2.2 关键发现

#### （1）特征重要性（随机森林）

| 排名 | 特征 | 重要性 |
|:---:|:---|---:|
| 1 | `demand_lag_1`（前 1 小时需求） | **86.9%** |
| 2 | `pickup_hour`（小时） | 4.6% |
| 3 | `demand_lag_24`（前 24 小时需求） | 2.6% |
| 4 | `demand_lag_6`（前 6 小时需求） | 1.6% |
| 5 | `demand_lag_12`（前 12 小时需求） | 1.1% |

> **洞察**：前一小时的 demand 是绝对主导特征，说明区域 161 的出行需求具有**强自回归特性**——上一小时的需求几乎决定了下一小时。这意味着需求模式在短期内高度连续，突发跳变较少。

#### （2）模型表现观察

- **日内潮汐捕捉**：两种模型均精准复现了"凌晨低谷 → 早高峰爬升 → 下午高峰 → 晚间回落"的完整周期。
- **随机森林更稳健**：RMSE 显著低于神经网络，说明其对极端偏差的控制能力更强。
- **神经网络高值发散**：在晚高峰（需求 > 500）的少数时点上，神经网络存在系统性低估（散点图右上角偏离完美预测线）。

---

## 3. 可视化图表

| 图表文件 | 说明 |
|:---|:---|
| `outputs/M3_A_nn_loss_curve.png` | PyTorch 训练 Loss 曲线（1000 epoch，MSE 收敛至 ~0.007） |
| `outputs/M3_A_timeseries_comparison.png` | 测试集 145 小时真实值 vs 预测值时序对比 |
| `outputs/M3_A_prediction_scatter.png` | 预测值 vs 真实值散点图（含完美预测线） |
| `outputs/M3_A_metrics_comparison.png` | MAE / RMSE 指标对比柱状图 |

---

## 4. 两种方法优劣分析

### 4.1 PyTorch 神经网络

**优势：**
1. **非线性拟合能力强**：通过多层 ReLU + Dropout，可捕捉小时、星期、滞后需求间的复杂交互。
2. **扩展性好**：未来可轻松引入 Embedding（区域 ID）、LSTM / Transformer 等时序模块。
3. **端到端优化**：基于梯度下降统一优化，适合大规模数据与在线学习场景。

**劣势：**
1. **对数据量仍敏感**：本任务约 576 条训练样本虽可用，但需仔细调参避免过拟合。
2. **超参数敏感**：学习率、网络层数、批次大小需调优，训练过程有波动。
3. **可解释性弱**：黑盒模型，难以直观解释特征贡献。

### 4.2 随机森林

**优势：**
1. **对 tabular 数据极其友好**：在结构化特征上通常表现稳定，调参成本低。
2. **免标准化**：无需特征缩放，工程成本低。
3. **可解释性强**：天然输出特征重要性，可直观看到 `lag_1`、`lag_24` 等关键滞后特征的主导作用。
4. **训练速度快**：并行建树，调参与迭代效率高。

**劣势：**
1. **难以捕捉复杂非线性交互**：虽然能处理非线性，但对特征间深层组合的建模能力弱于神经网络。
2. **外推能力有限**：对超出训练分布的极端值预测保守。
3. **时序依赖需人工构造**：原生无记忆能力，lag 特征必须手工设计。

### 4.3 方案 A 适配性结论

- 小时级预测将样本量提升至 **721 条**，两种模型都能有效学习，预测精度显著优于日度预测（样本从 15 条提升至 721 条）。
- 在此规模 tabular 数据上，**随机森林与神经网络性能接近，但随机森林更稳定、调参更少**，且以 86.9% 的特征重要性揭示了需求的强自回归本质。
- 若未来扩展至多月、多区域数据，神经网络配合时序结构（LSTM / Transformer）将展现更强的规模化与非线性建模潜力。

---

*报告结束*
