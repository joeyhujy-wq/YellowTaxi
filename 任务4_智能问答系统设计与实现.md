# 任务 4：NYC Yellow Taxi 智能问答系统

> **实现文件**: `main.py`  
> **报告文件**: `任务4_智能问答系统设计与实现.md`（本文档）  
> **运行方式**: `python main.py`（首次运行需输入 Kimi API Key）  
> **依赖**: `openai`, `pandas`, `numpy`, `matplotlib`, `torch`

---

## 1. 系统架构

```
用户输入（自然语言）
    │
    ▼
┌─────────────────┐
│   规则路由器     │  ← 关键词/正则匹配
│ rule_based_answer │
└─────────────────┘
    │
    ├─ 匹配成功 ──► 调用对应处理函数 ──► 返回数字结论 + 图表路径
    │
    └─ 匹配失败 ──► 调用 Kimi 大模型 API ──► 返回解释性回复
```

---

## 2. 支持的 6 种问题类型

| 编号 | 问题类型 | 匹配关键词 | 处理函数 | 输出内容 |
|:---:|:---|:---|:---|:---|
| 1 | **时段查询** | `点`、`时`、`上午`、`下午`、`晚上`、`凌晨` + 区域ID | `handle_time_query` | 特定区域/全区域在某小时的订单量 + 分小时柱状图 |
| 2 | **区域排名** | `排名`、`最高`、`最多`、`前几`、`热门`、`top` | `handle_zone_ranking` | TOP N 区域列表 + 参考图表路径 |
| 3 | **需求预测** | `预测`、`会有多少`、`估计`、`forecast`（排除含"钱/费用"的问句） | `handle_demand_prediction` | 基于历史均值的预测值 + 波动范围 |
| 4 | **费用估算** | `多少钱`、`费用`、`价格`、`fare`、`cost` | `handle_fare_estimate` | 平均费用、中位数、距离、费用区间 |
| 5 | **速度/拥堵查询** | `速度`、`拥堵`、`堵`、`畅通`、`mph`、`快慢` | `handle_speed_query` | 中位平均速度、最拥堵/最通畅时段 |
| 6 | **数据概览** | `数据集`、`规模`、`总订单`、`概览`、`overview` | `handle_overview` | 数据总量、区域数、时间范围、均值统计 |

---

## 3. 自然语言处理策略

### 3.1 关键词提取

系统通过多层解析函数从自然语言中提取结构化参数：

| 提取目标 | 方法 | 示例 |
|:---|:---|:---|
| **区域 ID** | 正则匹配：`区域\s*(\d+)`、`zone\s*(\d+)`、`(\d+)\s*区` 等 | "区域161" → 161 |
| **小时** | 阿拉伯数字+`点/时`，或中文数字映射 | "上午10点" → 10；"下午三点" → 15 |
| **TOP N** | 正则匹配：`前\s*(\d+)`、`top\s*(\d+)` | "前5个" → 5；默认返回 5 |
| **距离** | 正则：`(\d+(?:\.\d+)?)\s*英里` | "5英里" → 5.0 |

### 3.2 路由优先级设计

为避免一个问句触发多个规则，系统按以下优先级顺序匹配：

```
数据概览 > 区域排名 > 速度/拥堵 > 费用估算 > 需求预测 > 时段查询（兜底）
```

**关键迭代**：初始版本将"需求预测"放在"费用估算"之前，导致"从区域161到区域237大概多少钱"被"大概多少"误匹配为预测问题（因缺少小时参数返回 None）。修复后将**费用估算前置**，并在需求预测中增加排除条件（`if not any(k in q for k in ['钱', '费用', '价格'])`），解决了交叉匹配问题。

---

## 4. Kimi 大模型接入

### 4.1 API 配置

- **Base URL**: `https://api.moonshot.cn/v1`
- **模型**: `moonshot-v1-8k`
- **鉴权**: 从环境变量 `MOONSHOT_API_KEY` 读取，未设置时命令行提示输入

### 4.2 调用逻辑

```python
if rule_based_answer(question) is not None:
    return 规则匹配结果  # 数字结论 + 图表路径
else:
    return kimi_chat(question)  # 大模型解释性回复
```

---

## 5. System Prompt 设计与迭代

### v1.0 初版

```text
你是 NYC Yellow Taxi 数据分析助手。你拥有 2023 年 1 月纽约黄色出租车清洗后的
运营数据（约 304 万条订单），数据包含时间、空间、行程、费用、衍生特征等字段。

你的职责：
1. 对用户的出租车数据相关问题给出精准、简洁的数字结论。
2. 若问题涉及图表，请说明对应图表文件路径。
3. 若用户问题超出数据范围，请诚实说明无法回答。
4. 保持回答简短，优先给出数字结论，再辅以简要解释。
5. 回答请使用中文。
```

**问题**：过于通用，大模型在回答具体数据查询时容易" hallucinate "（编造数字），因为它无法直接访问本地 Parquet 文件。

### v1.1 迭代版（当前使用）

```text
你是 NYC Yellow Taxi 数据分析助手。你拥有 2023 年 1 月纽约黄色出租车清洗后的
运营数据（约 304 万条订单），数据包含以下字段：
- 时间: tpep_pickup_datetime, tpep_dropoff_datetime
- 空间: PULocationID（上车区域）, DOLocationID（下车区域）
- 行程: trip_distance（英里）, trip_duration_hour（小时）
- 费用: fare_amount, tip_amount, total_amount, congestion_surcharge, airport_fee
- 衍生特征: pickup_hour, pickup_weekday, is_weekend, is_rush_hour,
  avg_speed_mph, tip_percentage, cost_per_mile, is_airport_trip

你的职责：
1. 对用户的出租车数据相关问题给出精准、简洁的数字结论。
2. 若问题涉及图表，请说明对应图表文件路径（如 outputs/M2-1_hourly_demand.png）。
3. 若用户问题超出数据范围（如 2023 年 2 月、非 NYC 区域），
   请诚实说明无法回答，并给出最接近的推断或建议。
4. 保持回答简短，优先给出数字结论，再辅以简要解释。
5. 回答请使用中文。
```

**迭代要点**：
1. **显式枚举字段**：列出全部可用字段，降低模型编造字段名的概率。
2. **明确边界**：指明数据时间范围为"2023 年 1 月"、空间范围为"NYC"，让模型知道何时应该拒绝回答。
3. **图表路径引导**：提示模型在回答中引用 `outputs/` 目录下的图表，与本地可视化成果联动。
4. **回答格式约束**：要求"先数字、后解释"，确保输出结构统一。

---

## 6. 交互示例

### 示例 1：时段查询（规则匹配）

```
[用户] 区域161上午10点有多少订单
[系统 - 规则匹配] 区域 161 在 10:00 的订单量为 5,475 条。
[图表] outputs/qa_zone161_hourly.png
```

### 示例 2：区域排名（规则匹配）

```
[用户] 上车量最高的前3个区域
[系统 - 规则匹配] 上车量 TOP 3 区域:
  1. 区域 132: 157,268 条
  2. 区域 237: 147,070 条
  3. 区域 236: 137,629 条
[参考图表] outputs/M2-2a_top10_pickup.png
```

### 示例 3：需求预测（规则匹配）

```
[用户] 预测区域161下午3点的需求量
[系统 - 规则匹配] 区域 161 15:00 需求量预测:
  - 基于历史均值估计: 约 37 单/小时
  - 历史波动范围: 12 ~ 87 单/小时
  - 标准差: 19.3
[参考图表] outputs/M3_A_timeseries_comparison.png
```

### 示例 4：费用估算（规则匹配）

```
[用户] 从区域161到区域237大概多少钱
[系统 - 规则匹配] 区域 161 → 237 费用估算:
  - 平均总费用: $16.57
  - 中位数费用: $16.10
  - 平均距离: 1.11 英里
  - 费用范围: $12.30 ~ $21.23
[参考图表] outputs/M2-3a_distance_fare_scatter.png
```

### 示例 5：速度/拥堵查询（规则匹配）

```
[用户] 区域161早高峰平均速度多少
[系统 - 规则匹配] 区域 161 的整体中位平均速度为 8.7 mph。
```

### 示例 6：数据概览（规则匹配）

```
[用户] 数据集有多大
[系统 - 规则匹配] 数据集概览:
  - 总订单数: 3,040,881 条
  - 时间范围: 2023-01-01 ~ 2023-01-31
  - 上车区域数: 257 个
  - 下车区域数: 261 个
  - 平均行程距离: 3.30 英里
  - 平均总费用: $23.04
```

### 示例 7：开放式问题（Kimi 兜底）

```
[用户] 为什么纽约出租车的小费比例比餐厅高？
[系统 - 规则未匹配，调用 Kimi 大模型...]
[Kimi] 纽约出租车的小费比例较高主要有以下几个原因：
  1. 信用卡支付系统默认提示 20-25% 小费选项；
  2. 出租车费用本身包含了拥堵附加费、机场费等固定项目，
     部分乘客可能按总额比例给出小费；
  3. 服务场景差异：出租车司机承担导航、搬运行李等额外劳动...
  （注：本数据集 2023-01 的中位小费比例为 23.9%，高于餐厅常规 15-20%。）
```

---

## 7. 技术亮点与反思

| 亮点 | 说明 |
|:---|:---|
| **双层架构** | 规则引擎优先处理结构化查询（快、准、零成本），Kimi 兜底处理开放式问题（灵活、自然），兼顾效率与智能。 |
| **关键词提取模块化** | `parse_zone` / `parse_hour` / `parse_top_n` 等函数独立封装，便于扩展新 question type。 |
| **图表路径联动** | 规则回答中自动附带相关图表文件路径，实现"数字结论 + 可视化证据"的完整输出。 |
| **路由防冲突** | 通过优先级排序 + 排除条件（如"大概多少钱"排除需求预测），解决规则交叉匹配问题。 |

| 可改进方向 | 说明 |
|:---|:---|
| **语义匹配增强** | 当前基于关键词匹配，未来可引入 Embedding + 相似度阈值，支持更灵活的同义表达（如"打车费"/"乘车费用"/"fare"）。 |
| **多轮对话** | 当前为单轮问答，可扩展对话状态管理，支持追问（如"那区域237呢？"指代上文的问题类型）。 |
| **LLM 函数调用** | 可将规则函数注册为 Kimi 的 `function calling` 工具，让大模型自主决定是否调用本地数据函数，进一步融合规则与智能。 |

---

## 8. 交付物说明

| 文件 | 说明 |
|:---|:---|
| `main.py` | 命令行智能问答系统主程序（含规则引擎 + Kimi API 接入） |
| `任务4_智能问答系统设计与实现.md` | 本报告：系统架构、Prompt 迭代、交互示例 |

---

## 9. 运行方式

```bash
# 方式 1：环境变量传入 API Key（推荐）
set MOONSHOT_API_KEY=your-key-here
python main.py

# 方式 2：运行时交互输入
python main.py
# 提示 "请输入 Kimi (Moonshot) API Key:" 时粘贴即可
```

进入交互循环后输入问题即可，输入 `quit` 或 `exit` 退出。

---

## 10. 关键设计决策

1. **双层架构**：规则引擎优先处理结构化查询（快、准、零成本），Kimi 兜底处理开放式问题（灵活、自然），兼顾效率与智能。
2. **路由优先级设计**：`数据概览 > 区域排名 > 速度/拥堵 > 费用估算 > 需求预测 > 时段查询`。关键迭代：将"费用估算"置于"需求预测"之前，避免"大概多少钱"被"大概多少"误匹配为预测问题。
3. **关键词提取模块化**：`parse_zone` / `parse_hour` / `parse_top_n` 独立封装，便于扩展新 question type。
4. **图表路径联动**：规则回答中自动附带相关图表文件路径，实现"数字结论 + 可视化证据"的完整输出。

---

*报告结束*
