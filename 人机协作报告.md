# 人机协作报告：NYC Yellow Taxi 数据分析项目
# 胡家瑜 25306025

> **项目**: NYC Yellow Taxi 数据清洗、可视化、预测与智能问答系统  
> **协作 AI**: Kimi Code CLI  
> **总工作量**: 4 个模块（M1–M4），约 3,500 行代码，10+ 张图表，4 份分析报告

---

## 一、AI 交互日志

### M1：数据清洗与特征工程

| 轮次 | 关键 Prompt | AI 输出摘要 | 我的决策 |
|:---:|:---|:---|:---|
| 1 | "加载数据，生成数据质量报告；清洗数据并在注释中说明每步策略的理由；提取时间特征并设计至少 2 个衍生特征" | 生成了完整的 `taxi_analysis.py`，包含缺失率统计、IQR 异常值检测、10 步清洗策略（附理由注释）、4 个衍生特征（avg_speed_mph、tip_percentage、cost_per_mile、is_airport_trip） | **采用为主，局部修改**。AI 对清洗策略的理由说明非常到位，我保留了全部 10 步。额外要求保存为 Parquet，AI 直接执行。 |
| 2 | "把以上分析过程及结果写进一个 md 文件" | 生成了 `yellow_taxi_analysis_report.md`，结构完整，包含表格与代码块 | **直接采用**。报告格式规范，无需修改。 |

**AI 犯错案例 —— 中文字体渲染**
- **错误表现**: M2 可视化阶段，matplotlib 图表中的中文标题全部变成方框或触发 `Glyph missing from font(s) Arial` 警告。
- **根因**: AI 初始方案仅设置 `plt.rcParams['font.sans-serif'] = ['Microsoft YaHei']`，但在 Windows + seaborn 环境下，部分渲染路径（如 `fig.canvas.draw()`）仍会回退到 Arial。
- **发现过程**: 运行脚本后控制台大量 warning，打开 PNG 文件发现中文确实缺失。
- **修正方案**: 第一轮修复尝试 `font_manager.fontManager.addfont()` + `plt.rcParams['font.family'] = 'Microsoft YaHei'` 仍部分失败；第二轮改为**显式 `FontProperties(fname=font_path)` 传入每个 `set_title`/`set_xlabel`**，彻底解决问题。AI 在第二轮迭代中正确实现了该方案。

### M2：可视化分析

| 轮次 | 关键 Prompt | AI 输出摘要 | 我的决策 |
|:---:|:---|:---|:---|
| 1 | "完成 4 项分析，每项至少 1 张图表，保存至 outputs/" | 生成了 `taxi_visualization_m2.py`，含 10 张图（折线、面积、柱状、热力、散点、箱线、小提琴） | **采用并微调**。seaborn 0.13 的 `palette` 参数在无需 `hue` 时触发 FutureWarning，我要求 AI 补充 `hue=...` 并 `legend=False`，AI 正确修复。 |
| 2 | "把核心洞察速览存为一个 md 文件" | 生成了 `M2_insights_summary.md` | **直接采用**。 |

### M3：需求预测

| 轮次 | 关键 Prompt | AI 输出摘要 | 我的决策 |
|:---:|:---|:---|:---|
| 1 | "用 PyTorch 构建神经网络，预测区域 161 工作日上午 10–12 点需求量；与随机森林对比" | 生成了 `taxi_demand_prediction.py`，但按**天**聚合后样本仅 15 条 | **拒绝并迭代**。我发现样本量过小（训练 12 / 测试 3），要求 AI 分析"哪个区域哪个时段数据最多"。 |
| 2 | "请帮我选择哪个区域哪个时间段的数据最多" | AI 分析出：按天聚合瓶颈在"只有 1 个月数据"，提出**方案 A（小时级预测，745 条样本）**、方案 B（全区域小时级，5 万条）、方案 C（全天日度，32 条） | **采用方案 A**。小时级预测既聚焦热门区域 161，又将样本量提升至 721 条，训练/测试达到 576/145。 |
| 3 | "方案 A 吧" | 重写为 `taxi_demand_prediction_hourly.py`，14 维特征、PyTorch NN vs RF、Loss 曲线、时序对比图 | **采用**。RF 的 `demand_lag_1` 重要性高达 86.9%，揭示了强自回归特性，这一发现很有价值。 |

### M4：智能问答系统

| 轮次 | 关键 Prompt | AI 输出摘要 | 我的决策 |
|:---:|:---|:---|:---|
| 1 | "实现命令行问答循环，支持不少于 5 种问题类型，接入 kimi API，展示 System Prompt 迭代" | 生成了 `taxi_qa_system.py`，含 6 种问题类型、双层架构、System Prompt v1.0/v1.1 | **采用并迭代修复**。 |

**AI 犯错案例 —— 路由交叉匹配**
- **错误表现**: 用户输入"从区域161到区域237大概多少钱"，系统返回 `None`（匹配失败）。
- **根因**: 路由优先级中"需求预测"排在"费用估算"之前，而"大概多少钱"中的"大概多少"先触发了需求预测；`handle_demand_prediction` 因未解析到小时参数返回 `None`，导致整个问答链路断裂。
- **发现过程**: 单元测试 6 类问题时，该问句返回 `FAIL`。
- **修正方案**: 调整路由顺序为**费用估算 > 需求预测**，并在需求预测中增加排除条件 `if not any(k in q for k in ['钱', '费用', '价格'])`。修复后测试通过。

---

## 二、三阶段对比：以"车费–距离散点图"为例

### 1. Native 版（独立手写）

```python
import pandas as pd
import matplotlib.pyplot as plt

df = pd.read_parquet('yellow_tripdata_2023-01_cleaned.parquet')
sample = df.sample(50000)

plt.figure(figsize=(8,6))
plt.scatter(sample['trip_distance'], sample['total_amount'], 
            c=sample['pickup_hour'], cmap='viridis', alpha=0.5, s=10)
plt.xlim(0,30)
plt.ylim(0,150)
plt.colorbar(label='hour')
plt.title('Distance vs Fare')
plt.savefig('scatter.png')
```

**特点**: 代码极简，20 行以内完成核心逻辑。开发者需自行处理字体、布局、边界过滤，但对数据结构和可视化目标有绝对掌控。

### 2. Prompt 版（一次性 AI 辅助）

一次性输入："绘制行程距离–车费散点图，颜色表示上车小时，限制范围 0–30 英里、0–150 美元，保存 300dpi。"

AI 输出约 30 行代码，自动加入 `plt.tight_layout()`、`bbox_inches='tight'`、中文标题（但字体可能缺失）。开发者需运行后检查并手动修复字体警告。

**特点**: 开发速度快，一次性产出完整代码；但开发者对细节（如采样策略、异常值过滤）的掌控减弱，且需事后调试隐藏问题（如字体、seaborn 版本兼容性）。

### 3. Vibe 版（对话驱动，本项目实际模式）

```
[用户] "生成 M2-3a 距离-车费散点图"
[AI]  生成初版代码，含采样、颜色映射、范围限制
[用户] "图表中文显示为方框，修复字体"
[AI]  尝试 addfont + rcParams，仍部分失败
[用户] "改用 FontProperties 显式传入每个标题和标签"
[AI]  重写为显式字体方案，测试通过
[用户] "seaborn 报 FutureWarning，palette 需配合 hue"
[AI]  补充 hue 参数并设置 legend=False
[用户] "保存到 outputs/ 目录，dpi 300"
[AI]  调整路径与分辨率
```

**特点**: 通过 4–5 轮对话逐步逼近理想状态。开发者无需一次性构思全部细节，而是在"运行 → 观察 → 反馈 → 修正"的循环中推进。AI 在迭代中逐步理解了项目的编码规范（如显式字体、目录规范、中文注释）。

### 效率与理解深度对比

| 维度 | Native | Prompt | Vibe |
|:---|:---|:---|:---|
| **开发速度** | 慢（需手写全部细节） | 快（一次性生成） | 中等（多轮迭代） |
| **代码质量** | 高（完全符合开发者意图） | 中（可能遗漏边界情况） | 高（经过多轮打磨） |
| **理解深度** | 最深（完全自主设计） | 浅（一次性交互，AI 不了解上下文） | 较深（AI 逐步积累项目上下文） |
| **调试成本** | 低（自己写的 Bug 自己懂） | 高（需事后排查 AI 埋下的坑） | 低（Bug 在对话中即时暴露并修复） |
| **可维护性** | 高 | 中 | 高（代码经过双方确认） |

**结论**: Native 版适合对细节有极致要求的场景；Prompt 版适合快速原型验证；**Vibe 版在复杂项目中综合效率最高**——它既保留了开发者的设计主导权，又借助 AI 的代码生成能力大幅减少了重复劳动，同时通过即时反馈将 Bug 消灭在萌芽状态。

---

## 三、反思：AI 工具的能力边界

完成本次项目后，我对 AI 编程助手的能力边界有了更清醒的认识。

**第一，AI 在"结构化、模式化"任务上表现卓越，但在"隐性约束"上容易踩坑。** 例如，数据清洗的 10 步策略、特征工程的公式推导、正则匹配的函数封装，AI 都能高质量完成。然而，Windows 中文字体渲染、seaborn 版本兼容性、小样本下的过拟合风险等"环境依赖型"问题，AI 往往无法在第一次输出中预见。这些问题的共同特点是：它们不在 Prompt 的字面语义中，而是隐藏在开发环境、库版本、数据分布的细节里。这意味着开发者必须保持"运行时验证"的习惯，不能盲目信任 AI 的输出。

**第二，AI 的上下文窗口足够长，但"项目级连贯性"仍需人工维护。** 在本次对话中，AI 能够记住之前清洗数据的字段名、特征工程的衍生变量，并在后续可视化中复用。但当项目横跨多个文件（`taxi_analysis.py` → `taxi_visualization_m2.py` → `main.py`）时，AI 偶尔会遗漏文件名的变更（如最后要求重命名为 `main.py`，需要人工确认报告中的引用同步更新）。这说明 AI 的"记忆"是对话级别的，而非项目级别的，跨文件的一致性仍需开发者主动把控。

**第三，AI 是"放大器"而非"替代者"。** 它能将开发者的意图快速转化为代码，但无法替开发者定义"什么是好的分析"。例如，在 M3 中，AI 最初给出的日度预测方案样本不足，是我在追问下才切换到小时级预测；在 M4 中，System Prompt 的迭代方向（从通用定义到字段枚举）也是由我主导设计的。AI 的价值在于"执行"和"补全"，而"判断"和"方向"仍牢牢掌握在开发者手中。最好的协作模式不是"全交给 AI"，而是"我定方向，AI 填细节，双方共同打磨"。
