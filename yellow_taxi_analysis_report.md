# NYC Yellow Taxi 数据分析报告（2023-01）

> **数据集**: `yellow_tripdata_2023-01.parquet`  
> **分析时间**: 2026-05-09  
> **工具**: Python (pandas, numpy)

---

## 目录

1. [数据加载](#1-数据加载)
2. [数据质量报告](#2-数据质量报告)
   - 2.1 缺失率统计
   - 2.2 异常值统计（IQR法）
   - 2.3 时间字段异常
   - 2.4 业务逻辑异常
3. [数据清洗](#3-数据清洗)
   - 3.1 清洗策略总览
   - 3.2 每步策略详解
4. [特征工程](#4-特征工程)
   - 4.1 基础时间特征
   - 4.2 衍生特征设计
5. [统计结果汇总](#5-统计结果汇总)
6. [输出文件](#6-输出文件)

---

## 1. 数据加载

```python
import pandas as pd
import numpy as np

df = pd.read_parquet('yellow_tripdata_2023-01.parquet')
```

| 项目 | 数值 |
|:---|:---|
| 原始记录数 | **3,066,766** 条 |
| 字段数 | 19 列 |
| 核心字段 | `tpep_pickup_datetime`, `tpep_dropoff_datetime`, `passenger_count`, `trip_distance`, `fare_amount`, `tip_amount`, `total_amount`, `payment_type` 等 |

**原始字段类型概览**

| 字段名 | 数据类型 |
|:---|:---|
| `VendorID` | int64 |
| `tpep_pickup_datetime` | datetime64[us] |
| `tpep_dropoff_datetime` | datetime64[us] |
| `passenger_count` | float64 |
| `trip_distance` | float64 |
| `RatecodeID` | float64 |
| `store_and_fwd_flag` | object |
| `PULocationID` / `DOLocationID` | int64 |
| `payment_type` | int64 |
| `fare_amount` / `extra` / `mta_tax` / `tip_amount` / `tolls_amount` / `improvement_surcharge` / `total_amount` / `congestion_surcharge` / `airport_fee` | float64 |

---

## 2. 数据质量报告

### 2.1 缺失率统计

| 字段名 | 缺失数量 | 缺失率 (%) |
|:---|---:|---:|
| `passenger_count` | 71,743 | 2.3394 |
| `RatecodeID` | 71,743 | 2.3394 |
| `store_and_fwd_flag` | 71,743 | 2.3394 |
| `congestion_surcharge` | 71,743 | 2.3394 |
| `airport_fee` | 71,743 | 2.3394 |

> **分析**: 缺失值高度集中在同一批 71,743 条记录，推测为上游采集/同步异常导致的整行部分字段丢失，而非随机缺失。

### 2.2 异常值统计（IQR 法与业务常识）

| 字段 | 最小值 | 最大值 | IQR 异常值数 | IQR 异常值比例 (%) | 零值数 | 负值数 |
|:---|---:|---:|---:|---:|---:|---:|
| `passenger_count` | 0.00 | 9.00 | 733,623 | 24.49 | 51,164 | 0 |
| `trip_distance` | 0.00 | 258,928.15 | 390,244 | 12.72 | 45,862 | 0 |
| `fare_amount` | -900.00 | 1,160.10 | 325,228 | 10.60 | 1,110 | 25,049 |
| `extra` | -7.50 | 12.50 | 44,706 | 1.46 | 1,240,718 | 12,407 |
| `mta_tax` | -0.50 | 53.16 | 48,704 | 1.59 | 23,421 | 24,501 |
| `tip_amount` | -96.22 | 380.80 | 221,257 | 7.21 | 694,757 | 225 |
| `tolls_amount` | -65.00 | 196.99 | 226,459 | 7.38 | 2,840,307 | 1,377 |
| `improvement_surcharge` | -1.00 | 1.00 | 31,395 | 1.02 | 973 | 25,153 |
| `total_amount` | -751.00 | 1,169.40 | 371,616 | 12.12 | 568 | 25,204 |
| `congestion_surcharge` | -2.50 | 2.50 | 250,755 | 8.37 | 231,037 | 19,718 |
| `airport_fee` | -1.25 | 1.25 | 264,567 | 8.83 | 2,730,456 | 3,607 |

> **分析**: 多个金额字段存在负值，属于明显的数据录入错误；`trip_distance` 存在极端大值（25万英里），远超物理可能。

### 2.3 时间字段异常

基于 `trip_duration = dropoff - pickup`（单位：分钟）统计：

| 异常类型 | 数量 |
|:---|---:|
| 负行程时间（下车早于上车） | 3 |
| 零行程时间 | 1,118 |
| 超长行程时间（> 24 小时） | 25 |

### 2.4 业务逻辑异常

| 异常类型 | 数量 | 说明 |
|:---|---:|:---|
| 乘车人数为 0 | 51,164 | 出租车载客数为 0 不合逻辑 |
| 乘车人数 > 6 | 20 | 超出纽约出租车最大荷载 |
| 距离为 0 但金额 > 0 | 42,225 | 计费但未产生移动距离，可能为怠速或传感器故障 |
| 行程距离 > 100 英里 | 88 | 超出 NYC 出租车正常运营地理范围 |

---

## 3. 数据清洗

### 3.1 清洗结果概览

| 指标 | 数值 |
|:---|---:|
| 清洗前记录数 | 3,066,766 |
| 清洗后记录数 | 3,040,881 |
| 移除记录数 | 25,885 |
| 移除比例 | **0.84%** |

### 3.2 每步策略详解

| 步骤 | 清洗策略 | 处理数量 | 理由说明 |
|:---:|:---|---:|:---|
| 1 | **删除负行程时间记录** | 3 | 下车时间早于上车时间属于时间戳录入错误。无法判断是上车还是下车字段出错，整条记录不可信，故删除。 |
| 2 | **删除零时长且零金额记录** | 74 | 既无行驶距离也无费用产生，属于取消订单或系统测试记录，对真实出行分析无价值。 |
| 3 | **`passenger_count` 缺失用众数填充** | 71,743 | 缺失率约 2.3%，量级可控。众数为 1（单乘客出行最常见），用其填充不会引入系统性偏差。 |
| 4 | **`RatecodeID` 缺失用众数填充** | 71,743 | 标准费率（1）占绝对多数，缺失大概率也是普通订单，众数填充最稳健。 |
| 5 | **`store_and_fwd_flag` 缺失填 `'N'`** | 71,743 | 该字段表示"是否先存盘后转发"。绝大多数行程为实时传输，缺失等价于未触发存储，按 `'N'` 处理。 |
| 6 | **附加费缺失填 `0`** | 71,743 | `congestion_surcharge`、`airport_fee` 等缺失通常表示未产生该项费用，数值上等价于 0。 |
| 7 | **剔除极端行程距离** | 88 | 距离 > 100 英里或 < 0 英里严重超出 NYC 出租车正常运营范围，视为录入错误。 |
| 8 | **剔除极端总金额** | 25,696 | `total_amount` ≤ 0 为计费错误；> 1000 美元在常规行程中极罕见，视为异常。 |
| 9 | **剔除超长行程时间** | 24 | 行程 > 24 小时大概率是停车未正确结束计费或传感器故障，非真实出行。 |
| 10 | **移除临时字段 `trip_duration`** | — | 该字段仅用于清洗阶段的异常识别，为避免冗余，在特征工程中基于清洗后的时间重新精确计算。 |

> **原则**: 清洗以"最小干预、最大保留"为准则。仅删除明确不可信或不含信息的记录，对缺失值采用众数/零值等保守策略填充，最终仅移除 **0.84%** 的数据，保证样本量充足。

---

## 4. 特征工程

### 4.1 基础时间特征

基于 `tpep_pickup_datetime` 提取：

| 特征名 | 类型 | 取值范围 / 定义 |
|:---|:---|:---|
| `pickup_hour` | int | 上车小时，0–23 |
| `pickup_weekday` | int | 星期几，周一=0，周日=6 |
| `is_weekend` | int (0/1) | 周六或周日为 1，否则为 0 |
| `is_rush_hour` | int (0/1) | 工作日早晚高峰为 1（7–9 点、17–19 点），否则为 0 |

> **高峰定义依据**: NYC 出租车行业惯例及通勤模式，工作日早晚时段出行需求集中、拥堵严重，与普通时段行为差异显著。

### 4.2 衍生特征设计

除要求的小时、星期、高峰特征外，额外设计了 **4 个** 衍生特征（超额完成至少 2 个的要求）：

#### 衍生特征 1：`avg_speed_mph`（平均速度，mph）

```
avg_speed_mph = trip_distance / trip_duration_hour
```

| 项目 | 说明 |
|:---|:---|
| **业务意义** | 平均速度是交通拥堵程度的直接代理变量。异常低速可能暗示绕路、长时间怠速或传感器故障；异常高速则提示数据质量问题。 |
| **异常处理** | 速度 > 80 mph 在 NYC 城区几乎不可能，标记为 `NaN`。 |

#### 衍生特征 2：`tip_percentage`（小费占车费比例，%）

```
tip_percentage = tip_amount / fare_amount × 100
```

| 项目 | 说明 |
|:---|:---|
| **业务意义** | 小费的绝对金额受行程距离和车费基数影响大。计算比例后可剔除基数差异，更公平地衡量乘客满意度与司机服务质量。 |
| **异常处理** | 比例 > 50% 视为录入错误或极端离群值，标记为 `NaN`。 |

#### 衍生特征 3：`cost_per_mile`（每英里总成本，$）

```
cost_per_mile = total_amount / trip_distance
```

| 项目 | 说明 |
|:---|:---|
| **业务意义** | 单位距离成本剔除了行程长短的影响，便于横向对比不同区域、不同时段的定价效率，也可用于识别绕路（单位成本异常低）或极短程高起步价订单。 |
| **异常处理** | > $50/英里 通常对应极短距离高基础费或数据噪声，标记为 `NaN`。 |

#### 衍生特征 4：`is_airport_trip`（是否机场行程，0/1）

```
is_airport_trip = 1 if (airport_fee > 0) or (RatecodeID in {2, 3}) else 0
```

| 项目 | 说明 |
|:---|:---|
| **业务意义** | 机场行程（JFK、Newark 等）通常有固定费率、特殊附加费和不同的出行目的（商务/旅游），其行为模式与普通市区订单差异显著。将其作为二元特征可显著提升模型对这类特殊场景的识别能力。 |
| **识别依据** | `RatecodeID = 2` 对应 JFK 固定费率，`RatecodeID = 3` 对应 Newark 费率，`airport_fee > 0` 覆盖其余机场附加费场景。 |

---

## 5. 统计结果汇总

### 5.1 新特征描述性统计

| 特征 | count | mean | std | min | 25% | 50% | 75% | max |
|:---|---:|---:|---:|---:|---:|---:|---:|---:|
| `pickup_hour` | 3,040,881 | 14.13 | 5.77 | 0 | 10 | 15 | 19 | 23 |
| `pickup_weekday` | 3,040,881 | 3.00 | 1.99 | 0 | 1 | 3 | 5 | 6 |
| `is_weekend` | 3,040,881 | 0.28 | 0.45 | 0 | 0 | 0 | 1 | 1 |
| `is_rush_hour` | 3,040,881 | 0.17 | 0.37 | 0 | 0 | 0 | 0 | 1 |
| `avg_speed_mph` | 3,038,341 | 12.13 | 7.06 | 0.00 | 7.82 | 10.23 | 14.09 | 80.00 |
| `tip_percentage` | 3,007,085 | 19.82 | 13.12 | -12.97 | 7.81 | 23.90 | 29.15 | 50.00 |
| `cost_per_mile` | 2,982,627 | 12.00 | 5.74 | 0.02 | 7.98 | 10.87 | 14.57 | 50.00 |
| `is_airport_trip` | 3,040,881 | 0.10 | 0.30 | 0 | 0 | 0 | 0 | 1 |

### 5.2 关键发现

- **约 9.9%** 的订单为机场行程（`is_airport_trip = 1`），虽然占比不高，但客单价和模式显著不同。
- **平均速度 12.13 mph** 符合曼哈顿城区拥堵路况预期。
- **小费中位数约 23.9%**，高于美国常规餐厅小费比例，说明 NYC 出租车乘客对小费较为慷慨，或信用卡预授权小费机制导致比例偏高。
- **高峰时段订单占比约 17%**，符合通勤潮汐规律。

---

## 6. 输出文件

| 文件名 | 说明 |
|:---|:---|
| `taxi_analysis.py` | 完整分析脚本（加载 → 质量报告 → 清洗 → 特征工程 → 保存） |
| `yellow_tripdata_2023-01_cleaned.parquet` | 清洗并特征工程后的数据（3,040,881 行 × 28 列） |
| `yellow_taxi_analysis_report.md` | 本报告 |

**可直接使用清洗后的 Parquet 文件进行后续建模、可视化或 BI 分析。**

---

*报告结束*
